# React + Vite + PostgreSQL

AppThrust application template. The container listens on port 5173.

## Local Docker check

```bash
docker build -t template-react-vite:local .
docker run --rm -p 5173:5173 template-react-vite:local
```
